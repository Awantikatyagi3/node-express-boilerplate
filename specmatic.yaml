version: "3"

systemUnderTest:
  service:
    definitions:
      - definition:
          source:
            filesystem:
              directory: "."
          specs:
            - "openapi.yaml"
    runOptions:
      $ref: "#/components/runOptions/test"
components:
  runOptions:
    test:
      openapi:
        type: test
        baseUrl: "http://node-app:3000/v1"
